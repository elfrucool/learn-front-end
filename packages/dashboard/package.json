{
  "name": "dashboard",
  "version": "1.0.0",
  "description": "main application",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "devDependencies": {},
  "scripts": {
    "build": "rspack build --config rspack.config.library.js",
    "watch": "rspack build --config rspack.config.library.js --watch",
    "clean": "rm -rvf dist dist-standalone",
    "start": "rspack serve --config rspack.config.standalone.js",
    "test": "echo 'not implemented'"
  },
  "author": "",
  "license": "ISC"
}
